package zone.bonker.mythbound_core.client.gui;

import zone.bonker.mythbound_core.data.CharacterBuild;

public interface RefreshWithCharacterBuild {
    void refreshWidgets(CharacterBuild newCharacterBuild);
}
